extends layout

block content
  head
    h1= title
    p Welcome to Questions
  body
    ol
      each question in questions
        li.active
          a(href= '/question/'+  question.id) #{question.text}
          p #{question.difficulty}
